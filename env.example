# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# Option 1: Use individual connection parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=groupfund
DB_USER=postgres
DB_PASSWORD=your_password_here

# Option 2: Use connection string (common on Render, Heroku, etc.)
# DATABASE_URL=postgresql://user:password@host:port/database_name

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=10000d

# OTP Configuration
OTP_EXPIRY_MINUTES=10

# Resend Email Configuration (for OTP and notifications)
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM=GroupFund <onboarding@resend.dev>

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:19006

# Expo Push Notifications
# Note: Expo push notifications work without additional API keys in development
# For production with Android (FCM), you need to:
# 1. Configure FCM server key in your Expo project dashboard (https://expo.dev)
# 2. Get your Expo access token from https://expo.dev/accounts/[your-account]/settings/access-tokens
# EXPO_ACCESS_TOKEN=your_expo_access_token_here (optional, but recommended for production and FCM support)

# Migration Secret Token (for API-based migrations on free tier)
# MIGRATION_SECRET_TOKEN=your_secret_token_here (generate with: openssl rand -base64 32)

# Render Service URL (for keep-alive cron job to prevent sleeping)
# RENDER_SERVICE_URL=https://your-app-name.onrender.com

# Deep Link Configuration
# Deep link scheme (should match your Expo app configuration in app.json)
# Example: groupfund, myapp, etc.
DEEP_LINK_SCHEME=groupfund

# Universal Link URL (for iOS 9+ deep linking)
# Should be your domain with the deep link path
# Example: https://groupfund.app/g
UNIVERSAL_LINK_URL=https://groupfund.app/g

# App Store URLs (update with your actual app store links)
# iOS App Store URL (get from App Store Connect)
IOS_APP_STORE_URL=https://apps.apple.com/app/groupfund/idYOUR_APP_ID

# Android Play Store URL (get from Google Play Console)
ANDROID_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=com.groupfund.app

# Universal Links Configuration
# Apple Developer Team ID (found in Apple Developer account: https://developer.apple.com/account)
# Format: ABC123DEF4 (10 characters, alphanumeric)
APPLE_TEAM_ID=YOUR_TEAM_ID_HERE

# Android SHA-256 Certificate Fingerprint
# Get this from:
# 1. Google Play Console → Your App → Release → Setup → App signing (after upload)
# 2. Or from your keystore: keytool -list -v -keystore /path/to/keystore.jks -alias your-alias
# Format: XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX
# (64 hex characters, with or without colons)
ANDROID_SHA256_FINGERPRINT=REPLACE_WITH_YOUR_SHA256_FINGERPRINT
